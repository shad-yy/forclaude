# Smart Live TV - Environment Example for TheSportsDB

# Required in production, defaults to 'dev' in development
JWT_SECRET=dev

# TheSportsDB API Key; defaults to '1' (free tier) if not set
THESPORTSDB_API_KEY=123

# Optional
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
